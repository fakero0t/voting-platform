# Copy to .env (or set these in your host's dashboard) before going live.
PORT=3000
ADMIN_PASSWORD=change-me-before-sharing
# Where the SQLite file lives. On a cloud host, point this at a persistent disk.
DB_PATH=./data/voting.db
NODE_ENV=production
